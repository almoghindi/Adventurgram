apiVersion: apps/v1
kind: Deployment
metadata:
  name: userprofiles-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-profiles
  template:
    metadata:
      labels:
        app: user-profiles
    spec:
      containers:
        - name: user-profiles
          image: yonraz/ag-user-profiles
          env:
            - name: PSQL_URI
              value: ""
            - name: JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_KEY
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: user-profiles-srv
spec:
  selector:
    app: user-profiles
  type: ClusterIP
  ports:
    - name: user-profiles
      protocol: TCP
<<<<<<< HEAD
      port: 3000
      targetPort: 3000
=======
      port: 8080
      targetPort: 8080
>>>>>>> 90cf398c56c0b5db335fc966a67b52f59658bbf8
