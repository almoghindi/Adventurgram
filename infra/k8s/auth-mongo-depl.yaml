apiVersion: apps/v1
kind: Deployment
metadata:
  name: ag-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ag-auth-mongo
  template:
    metadata:
      labels:
        app: ag-auth-mongo
    spec:
      containers:
        - name: auth-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: ag-auth-mongo-srv
spec:
  selector:
    app: ag-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
